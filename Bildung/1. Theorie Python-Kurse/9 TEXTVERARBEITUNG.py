# 9.1 Unicode-Nummern für Zeichen
for buchsctaben in 'Abend':
    print(ord(buchsctaben), end = " ") # Result: 65 98 101 110 100

print(chr(65)) # Result A

print(chr(0x1F47D)) # Result: 👽
#
# \\ – Обратный слэш. Используется для экранирования самого себя, чтобы его можно было использовать в строках без интерпретации как управляющего символа.
#
# \n – Символ новой строки (перевод строки). Используется для перехода на новую строку в текстовых данных.
#
# \" – Экранирование двойной кавычки. Позволяет включать кавычки в строки, не завершая их преждевременно.
#
# \N{Name} – Вставка символа Юникода по его имени. Например, \N{LATIN SMALL LETTER A} вставит букву "a".
#
# \uxxxx – Вставка символа Юникода по его коду в 16-битном формате (4 шестнадцатеричные цифры). Например, \u03A9 – это греческая буква Омега (Ω).

text = "banana"
print(text.count("a")) # Result: 3

text = "document.pdf"
print(text.endswith(".pdf")) # Проверяет, заканчивается ли строка s указанным suffix. Выдаст true or false

name = "Илья"
age = 30
print("Меня зовут {} и мне {} лет.".format(name, age)) # Форматирует строку с подстановкой значений

words = ["apple", "banana", "cherry"]
print(", ".join(words)) #Result: apple, banana, cherry

text = "I like Python"
print(text.replace("like", "love")) # Заменяет в строке все вхождения old на new.

text = "apple, banana, cherry"
print(text.split(", ")) # Разбивает строку на список подстрок по разделителю sep.

text = "Hello, world!"
print(text.startswith("Hello")) # Возвращает True or False, Проверяет, начинается ли строка s с prefix.

text = "  hello  "
print(text.strip()) # Удаляет начальные и конечные пробелы (или символы, если указаны).

table = str.maketrans("aeiou", "12345")  # Меняем гласные на цифры
text = "hello"
print(text.translate(table)) # Используется для замены символов по таблице d, созданной через str.maketrans().


# 9.6 Ausblick: Reguläre Ausdrücke
# Основные конструкции в регулярных выражениях
# Символы
#
# . – любой один символ
# \d – любая цифра (0-9)
# \w – любая буква или цифра (a-z, A-Z, 0-9, _)
# \s – любой пробельный символ (пробел, табуляция, перенос строки)
# Квантификаторы (указывают, сколько раз повторяется элемент)
#
# * – 0 или более раз
# + – 1 или более раз
# ? – 0 или 1 раз
# {n} – ровно n раз
# {n,} – n и более раз
# {n,m} – от n до m раз
# Группы и альтернативы
#
# [abc] – любой из символов a, b, c
# [^abc] – любой символ, кроме a, b, c
# (abc|def) – либо abc, либо def
# (group) – группировка
# Якоря (указывают на позицию в строке)
#
# ^ – начало строки
# $ – конец строки
# \b – граница слова
#
# Вот список специальных символов (Sonderzeichen) регулярных выражений с кратким описанием:
#
# 🔹 Квантификаторы (повторы)
# Символ	Описание
# *	0 или более повторений
# +	1 или более повторений
# ?	0 или 1 повторение
# {n}	Ровно n повторений
# {n,}	n и более повторений
# {n,m}	От n до m повторений
# *?	Ленивый * (минимально возможное число повторов)
# +?	Ленивый +
# {n,m}?	Ленивый {n,m}
# 🔹 Классы символов
# Символ	Описание
# \d	Цифра (0-9)
# \D	Не цифра (всё, кроме 0-9)
# \s	Пробельный символ (пробел, табуляция, \n)
# \S	Не пробел
# \w	Буква, цифра или _ (a-z, A-Z, 0-9, _)
# \W	Не буква, цифра или _ (например, @, !, #)
# 🔹 Якоря (anchors)
# Символ	Описание
# ^	Начало строки
# $	Конец строки
# \b	Граница слова
# \B	Не граница слова
# \Z	Конец строки или файла
# 🔹 Группы и альтернативы
# Символ	Описание
# (abc)	Группа (запоминает совпадение)
# (?:abc)	Негруппирующие скобки (не запоминает)
# (?=abc)	Позитивный lookahead (проверяет, что дальше abc, но не включает его)
# (?!abc)	Негативный lookahead (убеждается, что дальше не abc)
# (?<=abc)	Позитивный lookbehind (проверяет, что перед есть abc)
# (?<!abc)	Негативный lookbehind (убеждается, что перед нет abc)
# .	Любой один символ (кроме \n)
# \	Экранирование (\. ищет точку, а не "любой символ")

text = '{} hat {} Euro gewonnen.'
text.format('Tom', 1000)
print(text.format('Tom', 1000)) # Tom hat 1000 Euro gewonnen.
# oder
text = '''{gewinner} hat {preis} Euro gewonnen'''
print(text.format(preis = 1000, gewinner = 'Tom')) # Tom hat 1000 Euro gewonnen




