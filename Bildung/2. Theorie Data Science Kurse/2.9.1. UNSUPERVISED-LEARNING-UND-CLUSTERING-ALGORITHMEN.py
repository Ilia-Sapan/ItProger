# 📌 Обучение без учителя и метод K-средних: краткий конспект
# 🔹 Обучение без учителя (Unsupervised Learning)
# Вид машинного обучения, где нет размеченных данных (то есть нет заранее известных ответов).
# Модель сама ищет скрытые структуры и закономерности в данных.
# Основные задачи:
# ✅ Кластеризация – объединение похожих объектов в группы.
# ✅ Снижение размерности – удаление лишней информации для упрощения анализа.
# ✅ Поиск аномалий – обнаружение редких и необычных данных.
# 🔹 Метод K-средних (K-Means)
# Алгоритм кластеризации, который делит данные на K групп (кластеров).
# Требуется заранее задать количество кластеров K.
# 🔢 Алгоритм работы: 1️⃣ Выбираем случайные K центроидов (средних значений кластеров).
# 2️⃣ Каждую точку данных относим к ближайшему центроиду.
# 3️⃣ Обновляем центроиды как средние значения точек в каждом кластере.
# 4️⃣ Повторяем шаги 2 и 3, пока центроиды не перестанут меняться.
#
# 📌 Преимущества: ✅ Быстрый и простой
# ✅ Хорошо работает на больших объемах данных
#
# ⚠ Недостатки: ❌ Нужно заранее знать K
# ❌ Чувствителен к шуму и выбросам
# ❌ Может не найти оптимальные кластеры, если данные сложные
#
# 📊 Где применяется K-Means?
# 🔹 Группировка клиентов в маркетинге
# 🔹 Классификация изображений
# 🔹 Анализ текстов и поисковых запросов
#
# Простой пример в Python:
#
# from sklearn.cluster import KMeans
# import numpy as np
#
# # Данные: 10 точек в двумерном пространстве
# data = np.array([[1, 2], [2, 3], [3, 3], [8, 7], [8, 8], [7, 8], [2, 2], [3, 4], [9, 9], [10, 10]])
#
# # Создаем модель KMeans с 2 кластерами
# kmeans = KMeans(n_clusters=2, random_state=42)
# kmeans.fit(data)
#
# # Выводим кластеры и центры
# print("Кластеры:", kmeans.labels_)
# print("Центроиды кластеров:", kmeans.cluster_centers_)
# 📌 Итог:
# K-Means – один из базовых и популярных методов кластеризации,
# но для сложных данных могут потребоваться более продвинутые алгоритмы
# (DBSCAN, агломеративная кластеризация и др.).
#
# 📌 Разница между K-Means и K-Means++
# 🔹 Стандартный K-Means выбирает K начальных центроидов случайно, что может приводить к плохой инициализации и неустойчивым результатам.
# 🔹 K-Means++ улучшает этот процесс, выбирая начальные центроиды более осмысленно, что повышает точность и скорость сходимости.
#
# 🔹 Как работает K-Means++?
# 1️⃣ Выбираем первый центроид случайно из данных.
# 2️⃣ Для каждой оставшейся точки вычисляем расстояние до ближайшего уже выбранного центроида.
# 3️⃣ Чем дальше точка от уже выбранных центроидов, тем выше шанс ее выбрать в качестве нового центроида.
# 4️⃣ Повторяем шаги 2–3, пока не выбрано K центроидов.
# 5️⃣ Затем выполняем стандартный алгоритм K-Means.
#
# 🔹 В чем преимущества K-Means++?
# ✅ Более стабильные результаты – снижает вероятность плохого разбиения.
# ✅ Более быстрая сходимость – требует меньше итераций, чем обычный K-Means.
# ✅ Улучшает качество кластеров – начальные центроиды расположены разумнее.
#
import numpy as np
import matplotlib.pyplot as plt
from sklearn.cluster import KMeans
from sklearn.metrics import silhouette_score

# Данные для кластеризации
x = np.array([4, 5, 10, 4, 3, 11, 14, 6, 10, 12])
y = np.array([21, 19, 24, 17, 16, 25, 24, 22, 21, 21])

# Объединяем x и y в двумерный массив (матрицу признаков)
data = np.column_stack((x, y))

# Список для хранения силуэтных коэффициентов
silhouette_coefficients = []

# Перебираем количество кластеров от 2 до 8
for k in range(2, 9):
    km = KMeans(n_clusters=k, init="k-means++", random_state=42)  # Используем K-Means++
    km.fit(data)

    score = silhouette_score(data, km.labels_)  # Вычисляем силуэтный коэффициент
    silhouette_coefficients.append(score)

# Строим график зависимости силуэтного коэффициента от K
plt.plot(range(2, 9), silhouette_coefficients, marker="o", linestyle="-")
plt.xlabel("Количество кластеров (K)")
plt.ylabel("Силуэтный коэффициент")
plt.title("Выбор оптимального K с помощью силуэтного метода")
plt.show()






