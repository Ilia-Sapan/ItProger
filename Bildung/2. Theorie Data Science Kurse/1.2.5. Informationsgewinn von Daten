На этой странице объясняется информационный выигрыш (Information Gain, IG) — один из ключевых показателей, используемых в машинном обучении, особенно в алгоритмах построения деревьев решений.

1. Что такое информационный выигрыш?
Информационный выигрыш показывает, насколько знание определённого признака (feature) уменьшает неопределённость (энтропию) в данных.

Формула информационного выигрыша:

IG = sum(p[i] * I[i] for i in range(len(p)))
Где:

p[i] — вероятность события

I[i] — информация, связанная с этим событием.
2. Как это работает?
Пример: у нас есть база данных посетителей сайта, и мы хотим предсказать, совершит ли человек покупку. Возможные признаки (Features):

Время пребывания на сайте (долго/коротко).
Географическое происхождение (страна, город).
Возраст (молодой/взрослый/пожилой).
Чтобы выбрать лучший признак для классификации, мы рассчитываем информационный выигрыш для каждого:

Вычисляем энтропию до разделения.
Разбиваем данные по признаку (например, возрасту).
Вычисляем среднюю энтропию после разбиения.
Вычитаем новую энтропию из исходной – это и есть Information Gain.
Чем выше IG, тем больше знаний даёт нам этот признак, и тем полезнее он для классификации.

3. Итог
Информационный выигрыш показывает, насколько полезен признак для разделения данных.
Признак с наибольшим IG выбирается первым в дереве решений (например, в алгоритме ID3 или C4.5).
IG похож на энтропию, но показывает не уровень неопределённости, а насколько сильно признак её снижает.
👉 Чем больше IG, тем лучше признак для предсказаний!